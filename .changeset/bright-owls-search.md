---
'@mastra/code-sdk': minor
---

Added Parallel as a Mastra Code web search and extraction provider. When `PARALLEL_API_KEY` is set, Parallel takes priority over Tavily. Existing Tavily and model-native fallbacks remain available.
